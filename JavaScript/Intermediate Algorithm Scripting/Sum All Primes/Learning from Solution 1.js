function sumPrimes(num) {

}

sumPrimes(10);

// Make an array with all numbers from 2 to num

// Make a filter. For each value check if it is in the composite array.
// If it is not, add it to the prime array and add all multiples of it to the composite array

// Use reduce to sum all numbers in the prime array.